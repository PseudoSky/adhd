# workspace-workspace-base-vite-paths

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build workspace-workspace-base-vite-paths` to build the library.

## Running unit tests

Run `nx test workspace-workspace-base-vite-paths` to execute the unit tests via [Vitest](https://vitest.dev/).
