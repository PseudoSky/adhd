# workspace-workspace-base-tools

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build workspace-workspace-base-tools` to build the library.

## Running unit tests

Run `nx test workspace-workspace-base-tools` to execute the unit tests via [Vitest](https://vitest.dev/).
