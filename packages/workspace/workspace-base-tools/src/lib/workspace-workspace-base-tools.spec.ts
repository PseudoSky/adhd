import { workspaceWorkspaceBaseTools } from './workspace-workspace-base-tools';

describe('workspaceWorkspaceBaseTools', () => {
  it('should work', () => {
    expect(workspaceWorkspaceBaseTools()).toEqual(
      'workspace-workspace-base-tools'
    );
  });
});
