import { dispatchDispatchBaseTypes } from './dispatch-dispatch-base-types';

describe('dispatchDispatchBaseTypes', () => {
  it('should work', () => {
    expect(dispatchDispatchBaseTypes()).toEqual('dispatch-dispatch-base-types');
  });
});
