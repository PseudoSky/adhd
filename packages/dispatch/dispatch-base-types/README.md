# dispatch-dispatch-base-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dispatch-dispatch-base-types` to build the library.

## Running unit tests

Run `nx test dispatch-dispatch-base-types` to execute the unit tests via [Vitest](https://vitest.dev/).
