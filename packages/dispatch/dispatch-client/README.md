# dispatch-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dispatch-client` to build the library.

## Running unit tests

Run `nx test dispatch-client` to execute the unit tests via [Vitest](https://vitest.dev/).
