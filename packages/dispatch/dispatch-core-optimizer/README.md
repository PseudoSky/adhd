# dispatch-core-optimizer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dispatch-core-optimizer` to build the library.

## Running unit tests

Run `nx test dispatch-core-optimizer` to execute the unit tests via [Vitest](https://vitest.dev/).
