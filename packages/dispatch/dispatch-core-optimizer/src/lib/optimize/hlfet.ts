import type {
  DagSnapshot,
  DispatchUnit,
  IOptimizerDeps,
} from '@adhd/dispatch-spec';

export function hlfet(
  snapshot: DagSnapshot,
  deps: IOptimizerDeps
): DispatchUnit[] {
  return [];
}
