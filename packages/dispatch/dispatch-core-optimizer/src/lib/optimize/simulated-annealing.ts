import type {
  DagSnapshot,
  DispatchUnit,
  IOptimizerDeps,
} from '@adhd/dispatch-spec';

export function simulatedAnnealing(
  snapshot: DagSnapshot,
  deps: IOptimizerDeps
): DispatchUnit[] {
  return [];
}
