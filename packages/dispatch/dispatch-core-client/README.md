# dispatch-core-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dispatch-core-client` to build the library.

## Running unit tests

Run `nx test dispatch-core-client` to execute the unit tests via [Vitest](https://vitest.dev/).
