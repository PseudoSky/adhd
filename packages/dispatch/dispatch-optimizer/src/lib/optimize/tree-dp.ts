import type {
  DagSnapshot,
  DispatchUnit,
  IOptimizerDeps,
} from '@adhd/dispatch-spec';

export function treeDp(
  snapshot: DagSnapshot,
  deps: IOptimizerDeps
): DispatchUnit[] {
  return [];
}
