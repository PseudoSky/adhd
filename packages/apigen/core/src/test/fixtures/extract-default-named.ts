// Fixture: Shape 4 — default-export named function
// export default function foo(...)

export default function processOrder(orderId: string, quantity: number): { success: boolean } {
  return { success: true }
}
