{
  "name": "apigen-generator-nx",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/apigen/apigen-generator-nx/src",
  "projectType": "library",
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "preserveLocalDependencyProtocols": false,
      "manifestRootsToUpdate": ["{projectRoot}"]
    }
  },
  "tags": ["layer:logic", "platform:node"],
  "implicitDependencies": ["!workspace-base-vite-paths"],
  "targets": {
    "build": {
      "//": "Override of the `build` target inferred by @nx/js/typescript (see that plugin's registration in nx.json for why the inferred `tsc --build tsconfig.lib.json` cannot run in this workspace). The trailing cp calls reproduce the `assets` globs the retired tsc build executor copied (generator/executor schemas + templates); README/CHANGELOG are owned by the inferred `assets` target.",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": ".",
        "command": "node node_modules/typescript/bin/tsc -p {projectRoot}/tsconfig.lib.json --outDir {projectRoot}/dist --rootDir {projectRoot} --baseUrl {projectRoot} --composite false --noEmitOnError && cp {projectRoot}/*.md {projectRoot}/generators.json {projectRoot}/executors.json {projectRoot}/dist/ && cp {projectRoot}/src/generators/host/schema.json {projectRoot}/dist/src/generators/host/ && cp -R {projectRoot}/src/generators/host/__files__ {projectRoot}/dist/src/generators/host/ && cp {projectRoot}/src/generators/plugin/schema.json {projectRoot}/dist/src/generators/plugin/ && cp -R {projectRoot}/src/generators/plugin/__files__ {projectRoot}/dist/src/generators/plugin/ && cp {projectRoot}/src/executors/generate/schema.json {projectRoot}/dist/src/executors/generate/"
      }
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    }
  }
}
