{
  "name": "apigen-plugin-mcp",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "release": {
    "version": {
      "preserveLocalDependencyProtocols": false,
      "manifestRootsToUpdate": ["packages/apigen/apigen-plugin-mcp"]
    }
  },
  "tags": ["layer:logic", "platform:node"],
  "targets": {
    "test": {
      "dependsOn": ["lint", "^build"],
      "//inputs": "Exclude *.e2e.ts so editing a resource-consuming suite never invalidates the default lane's cache — it never runs those files (its vitest `include` is `src/**/*.spec.ts`, see vite.config.ts). Mirrors the workspace targetDefaults.test inputs plus the negative pattern.",
      "inputs": [
        "default",
        "!{projectRoot}/**/*.e2e.ts",
        "^production",
        {
          "externalDependencies": ["vitest", "better-sqlite3", "drizzle-orm"]
        }
      ]
    },
    "e2e": {
      "//": "RESOURCE-CONSUMING lane. Runs ONLY the `*.e2e.ts` suites extracted out of the default `test` target (real MCP servers on real ports driven over real HTTP by the SDK client transports, plus their spawnSync negative controls). Opt-in and deliberately NOT wired into any `test.dependsOn` nor into nx.json `targetDefaults`, so `nx affected -t test` / the pre-commit + pre-push hooks never reach it. `dependsOn: [\"build\"]` because it builds the package before exercising its real run() path. Its coverage dir is DISTINCT from the default lane's, and declares no dist-shaped output.",
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/apigen-plugin-mcp-e2e"
      ],
      "dependsOn": ["build"],
      "options": {
        "cwd": ".",
        "command": "vitest run --config {projectRoot}/vitest.e2e.config.ts"
      },
      "cache": false
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    }
  }
}
