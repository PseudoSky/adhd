{
  "name": "apigen-python-env",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/apigen/python-env/src",
  "projectType": "library",
  "release": {
    "version": {
      "generatorOptions": {
        "packageRoot": "{projectRoot}",
        "currentVersionResolver": "git-tag"
      }
    }
  },
  "tags": [
    "layer:logic",
    "platform:node"
  ],
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "//dependsOn": "No `build` dependency: the resource-consuming suite that read this project's OWN build (the published-layout checks read dist/python/) was extracted into the sibling `python-env.e2e.ts` file (see the `python-env.spec.ts` stub) and now runs only under the opt-in `e2e` target. No remaining default-lane spec reads dist/, so the default target no longer builds before testing. targetDefaults.test still contributes `^build` (dependency builds) + `lint`.",
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/python-env"
      ],
      "options": {
        "configFile": "packages/apigen/python-env/vite.config.ts"
      }
    },
    "e2e": {
      "//": "Resource-consuming lane: runs ONLY the `*.e2e.ts` suite extracted out of the default `test` target (real venv provisioning + `spawnSync` python probes, plus a `dist/` copy). Opt-in and deliberately NOT wired into any `test.dependsOn` nor into nx.json `targetDefaults`, so `nx affected -t test` / the pre-commit + pre-push hooks never reach it. `dependsOn: [\"build\"]` because the moved suite reads this package's OWN build (the published-layout checks read dist/python/, produced by this package's build). Its coverage dir is DISTINCT from the default lane's, and declares no dist-shaped output.",
      "executor": "@nx/vite:test",
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/python-env/e2e"
      ],
      "dependsOn": [
        "build"
      ],
      "options": {
        "configFile": "packages/apigen/python-env/vitest.e2e.config.ts",
        "passWithNoTests": true
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "inputs": [
        "production",
        "^production",
        "{workspaceRoot}/packages/apigen/python/**/*",
        "!{workspaceRoot}/packages/apigen/python/**/__pycache__/**",
        "!{workspaceRoot}/packages/apigen/python/**/*.pyc",
        "!{workspaceRoot}/packages/apigen/python/.pytest_cache/**",
        "!{workspaceRoot}/packages/apigen/python/build/**",
        "!{workspaceRoot}/packages/apigen/python/*.egg-info/**"
      ],
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/apigen/python-env/dist",
        "main": "packages/apigen/python-env/src/index.ts",
        "tsConfig": "packages/apigen/python-env/tsconfig.lib.json",
        "assets": [
          "packages/apigen/python-env/*.md"
        ]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "{projectRoot}/dist"
      },
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ]
    }
  }
}
