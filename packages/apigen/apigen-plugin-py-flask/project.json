{
  "name": "apigen-plugin-py-flask",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "release": {
    "version": {
      "generatorOptions": {
        "packageRoot": "packages/apigen/apigen-plugin-py-flask"
      }
    }
  },
  "tags": [
    "layer:logic",
    "platform:node"
  ],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/apigen/apigen-plugin-py-flask/dist",
        "emptyOutDir": true
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "//inputs": "Exclude *.e2e.ts so editing the resource-consuming suite never invalidates the default lane's cache — it never runs those files (its vitest `include` is `src/**/*.spec.ts`, see vite.config.ts) and the live-python suite now lives ONLY on the `e2e` target below.",
      "inputs": [
        "default",
        "!{projectRoot}/**/*.e2e.ts",
        "^production",
        "{workspaceRoot}/packages/apigen/python/**/*",
        "!{workspaceRoot}/packages/apigen/python/**/__pycache__/**",
        "!{workspaceRoot}/packages/apigen/python/**/*.pyc",
        "!{workspaceRoot}/packages/apigen/python/.pytest_cache/**",
        "!{workspaceRoot}/packages/apigen/python/build/**",
        "!{workspaceRoot}/packages/apigen/python/*.egg-info/**"
      ],
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/apigen-plugin-py-flask"
      ],
      "options": {
        "configFile": "packages/apigen/apigen-plugin-py-flask/vite.config.ts"
      }
    },
    "e2e": {
      "executor": "@nx/vite:test",
      "//": "RESOURCE-CONSUMING lane. Deliberately NOT part of `affected` and NOT referenced by any target's dependsOn or by targetDefaults — it runs ONLY on an explicit `nx run apigen-plugin-py-flask:e2e` (nx affected never selects it). It runs the sibling plugin.e2e.ts suite via vitest.e2e.config.ts. `dependsOn` is `^build` — the same upstream-build prerequisite the default `test` lane inherited from targetDefaults, which builds apigen-python-env (the managed interpreter the suite provisions + spawns at runtime). The suite never loads a built artifact of this package itself.",
      "dependsOn": ["^build"],
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/apigen-plugin-py-flask-e2e"
      ],
      "options": {
        "configFile": "packages/apigen/apigen-plugin-py-flask/vitest.e2e.config.ts",
        "passWithNoTests": true
      }
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "executor": "@nx/js:release-publish",
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    }
  }
}
