# apigen-runtime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build apigen-runtime` to build the library.

## Running unit tests

Run `nx test apigen-runtime` to execute the unit tests via [Vitest](https://vitest.dev/).
