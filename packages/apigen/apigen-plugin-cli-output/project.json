{
  "name": "apigen-plugin-cli-output",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "release": {
    "version": {
      "generatorOptions": {
        "packageRoot": "packages/apigen/apigen-plugin-cli-output"
      }
    }
  },
  "tags": [
    "layer:logic",
    "platform:node"
  ],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/apigen/apigen-plugin-cli-output/dist",
        "emptyOutDir": true
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "//dependsOn": "The resource-consuming run-cli-integration suite (real execFile spawns of the built entrypoint/apigen-cli CLI and of this package's own built dist) moved to the sibling run-cli-integration.e2e.ts with a cheap run-cli-integration.spec.ts stub. This default lane no longer spawns a built artifact and no longer declares `build` / `apigen-cli:build`; those resource prerequisites now live ONLY on the `e2e` target below. This target inherits the workspace targetDefaults.test dependsOn ([\"lint\", \"^build\"]).",
      "//inputs": "Exclude *.e2e.ts so editing the resource-consuming suite never invalidates the default lane's cache — it never runs those files (its vitest `include` is `src/**/*.spec.ts`, see vite.config.ts). Mirrors the workspace targetDefaults.test inputs plus the negative pattern.",
      "inputs": [
        "default",
        "!{projectRoot}/**/*.e2e.ts",
        "^production",
        {
          "externalDependencies": ["vitest", "better-sqlite3", "drizzle-orm"]
        }
      ],
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/apigen-plugin-cli-output"
      ],
      "options": {
        "configFile": "packages/apigen/apigen-plugin-cli-output/vite.config.ts"
      }
    },
    "e2e": {
      "executor": "@nx/vite:test",
      "//": "RESOURCE-CONSUMING lane. Deliberately NOT part of `affected` and NOT referenced by any target's dependsOn or by targetDefaults — it runs ONLY on an explicit `nx run apigen-plugin-cli-output:e2e` (nx affected never selects it). It runs the sibling run-cli-integration.e2e.ts suite via vitest.e2e.config.ts. `dependsOn` is exactly the resource prerequisites those tests need: `build` (the parity harness require()s this package's real built dist/index.js) and `apigen-cli:build` (the run-integration tests execFile the real built entrypoint/apigen-cli/dist/index.js).",
      "dependsOn": [
        "build",
        {
          "projects": ["apigen-cli"],
          "target": "build"
        }
      ],
      "outputs": [
        "{workspaceRoot}/coverage/packages/apigen/apigen-plugin-cli-output-e2e"
      ],
      "options": {
        "configFile": "packages/apigen/apigen-plugin-cli-output/vitest.e2e.config.ts",
        "passWithNoTests": true
      }
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "executor": "@nx/js:release-publish",
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    }
  }
}
