# shared-dispatch-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-dispatch-client` to build the library.

## Running unit tests

Run `nx test shared-dispatch-client` to execute the unit tests via [Vitest](https://vitest.dev/).
