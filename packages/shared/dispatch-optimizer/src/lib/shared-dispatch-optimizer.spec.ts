import { sharedDispatchOptimizer } from './shared-dispatch-optimizer';

describe('sharedDispatchOptimizer', () => {
  it('should work', () => {
    expect(sharedDispatchOptimizer()).toEqual('shared-dispatch-optimizer');
  });
});
