# shared-dispatch-optimizer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-dispatch-optimizer` to build the library.

## Running unit tests

Run `nx test shared-dispatch-optimizer` to execute the unit tests via [Vitest](https://vitest.dev/).
