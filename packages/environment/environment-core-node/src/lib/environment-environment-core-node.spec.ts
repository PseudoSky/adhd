import { environmentEnvironmentCoreNode } from './environment-environment-core-node';

describe('environmentEnvironmentCoreNode', () => {
  it('should work', () => {
    expect(environmentEnvironmentCoreNode()).toEqual(
      'environment-environment-core-node'
    );
  });
});
