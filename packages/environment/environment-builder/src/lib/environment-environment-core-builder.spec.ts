import { environmentEnvironmentCoreBuilder } from './environment-environment-core-builder';

describe('environmentEnvironmentCoreBuilder', () => {
  it('should work', () => {
    expect(environmentEnvironmentCoreBuilder()).toEqual(
      'environment-environment-core-builder'
    );
  });
});
