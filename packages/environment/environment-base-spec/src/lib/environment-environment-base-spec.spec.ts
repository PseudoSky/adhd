import { environmentEnvironmentBaseSpec } from './environment-environment-base-spec';

describe('environmentEnvironmentBaseSpec', () => {
  it('should work', () => {
    expect(environmentEnvironmentBaseSpec()).toEqual(
      'environment-environment-base-spec'
    );
  });
});
