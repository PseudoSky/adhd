{
  "name": "data-query-engine",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/data/data-query-engine/src",
  "projectType": "library",
  "release": {
    "version": {
      "generatorOptions": {
        "packageRoot": "dist/{projectRoot}",
        "currentVersionResolver": "git-tag"
      }
    }
  },
  "tags": [
    "platform:shared",
    "layer:data"
  ],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "dependsOn": [
        "^build",
        "lint"
      ],
      "options": {
        "outputPath": "dist/packages/data/data-query-engine",
        "main": "packages/data/data-query-engine/src/index.ts",
        "tsConfig": "packages/data/data-query-engine/tsconfig.lib.json",
        "assets": [
          "packages/data/data-query-engine/*.md"
        ]
      }
    },
    "version": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "scripts/version-packages.sh query packages/data/data-query-engine"
        ]
      }
    },
    "version-prerelease": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "scripts/version-packages.sh query packages/data/data-query-engine @adhd -prerelease"
        ]
      }
    },
    "publish": {
      "dependsOn": [
        "^version"
      ],
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "echo 'Running npm publish dry run'",
          "npm publish --dry-run dist/packages/data/data-query-engine"
        ]
      },
      "configurations": {
        "production": {
          "parallel": false,
          "commands": [
            "npm publish dist/packages/data/data-query-engine"
          ]
        }
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    }
  }
}
