# agent-agent-core-env

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build agent-agent-core-env` to build the library.

## Running unit tests

Run `nx test agent-agent-core-env` to execute the unit tests via [Vitest](https://vitest.dev/).
