{
  "name": "agent-engine-compiler",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/agent/agent-engine-compiler/src",
  "projectType": "library",
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "preserveLocalDependencyProtocols": false,
      "manifestRootsToUpdate": ["{projectRoot}"]
    }
  },
  "tags": ["layer:ai", "platform:node"],
  "targets": {
    "build": {
      "//": "Override of the `build` target inferred by @nx/js/typescript (see that plugin's registration in nx.json for why the inferred `tsc --build tsconfig.lib.json` cannot run in this workspace). The `drizzle/**/*` asset glob is covered by the inferred `assets` target.",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": ".",
        "command": "node node_modules/typescript/bin/tsc -p {projectRoot}/tsconfig.lib.json --outDir {projectRoot}/dist --rootDir {projectRoot} --baseUrl {projectRoot} --composite false --noEmitOnError"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/agent/agent-engine-compiler/tsconfig.typecheck.json --noEmit"
      }
    },
    "db:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit generate",
        "cwd": "packages/agent/agent-engine-compiler"
      }
    },
    "db:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit migrate",
        "cwd": "packages/agent/agent-engine-compiler"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist/packages/agent/agent-engine-compiler"
      }
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    },
    "test": {
      "//": "Override of the inferred `test` target (dependsOn only; executor/cache/outputs come from inference). compile-cli*.test.ts spawn the BUILT bin at {projectRoot}/dist/src/cli/compile.js and assert the migration folders exist at {projectRoot}/dist/drizzle — under this workspace's remodel that asset glob is owned by the inferred `assets` target (main's `@nx/js:tsc` `build` copied it inline). targetDefaults.test's `^build` builds only DEPENDENCIES, never this project's OWN dist/ or assets, so `build` + `assets` are declared. `lint`/`^build` repeated because an explicit dependsOn replaces the targetDefault.",
      "dependsOn": ["lint", "^build", "build", "assets"]
    }
  }
}
