# agent-mcp-budget

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build agent-mcp-budget` to build the library.

## Running unit tests

Run `nx test agent-mcp-budget` to execute the unit tests via [Vitest](https://vitest.dev/).
