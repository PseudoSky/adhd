# shared-agent-mcp-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-agent-mcp-types` to build the library.

## Running unit tests

Run `nx test shared-agent-mcp-types` to execute the unit tests via [Vitest](https://vitest.dev/).
