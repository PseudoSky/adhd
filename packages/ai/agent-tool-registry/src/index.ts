/**
 * @adhd/agent-tool-registry — tool registry stub.
 *
 * Scaffold placeholder. Implementation tracked in docs/plan/agent-tool-registry (state: pending).
 * Present so tsconfig.base.json path alias resolves to a real file and ESLint
 * module-boundary autofix does not crash with ENOENT.
 */

export const __apigen_pkg = '@adhd/agent-tool-registry';
