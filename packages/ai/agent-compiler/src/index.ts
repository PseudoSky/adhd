/**
 * @adhd/agent-compiler — composition and compile engine stub.
 *
 * This file is a scaffold placeholder. The full implementation is tracked in
 * docs/plan/agent-compiler (state: pending). It is present so that the
 * tsconfig.base.json path alias (`@adhd/agent-compiler`) resolves to a real
 * file and tooling that reads the source tree (e.g. @nx/enforce-module-boundaries
 * autofix) does not crash with ENOENT.
 */

export const __apigen_pkg = '@adhd/agent-compiler';
