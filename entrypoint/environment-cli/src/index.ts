/**
 * `@adhd/environment-cli` — public API surface.
 *
 * Re-exports the full apigen extraction surface from `api.ts`.
 * Every exported function corresponds to a CLI command.
 */
export * from './api';
