{
  "name": "apigen-cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "entrypoint/apigen-cli/src",
  "projectType": "library",
  "release": {
    "version": {
      "currentVersionResolver": "git-tag",
      "preserveLocalDependencyProtocols": false,
      "manifestRootsToUpdate": ["{projectRoot}"]
    }
  },
  "tags": ["layer:entrypoints", "platform:node"],
  "targets": {
    "test": {
      "//": "Default (in-process, source-level) lane. Most resource-consuming specs — the integration suites that spawn the built CLI bin, and the java-javalin parity host that needs `apigen-java:package` — live in sibling `*.e2e.ts` files under the on-demand `e2e` target. One exception remains here and is why this target also builds itself: `src/test/e2e/cjs-import-meta-url.spec.ts` reads and spawns this package's own built `dist/index.js` (BUG-BUILD-002 acceptance), so `build` (not just `^build`, which builds only upstream deps) is a hard dependency — without it the spec ENOENTs on a clean checkout. Override of the inferred `test` target carries only `dependsOn` (`lint`/`build`/`^build`) plus `inputs` excluding `*.e2e.ts`; executor/cache/outputs come from inference.",
      "dependsOn": ["lint", "build", "^build"],
      "//inputs": "Exclude *.e2e.ts so editing a resource-consuming suite never invalidates the default lane's cache — it never runs those files (its vitest `include` is `src/**/*.spec.ts`, see vite.config.ts). Mirrors the workspace targetDefaults.test inputs plus the negative pattern.",
      "inputs": [
        "default",
        "!{projectRoot}/**/*.e2e.ts",
        "^production",
        {
          "externalDependencies": ["vitest", "better-sqlite3", "drizzle-orm"]
        }
      ]
    },
    "e2e": {
      "executor": "nx:run-commands",
      "//": "RESOURCE-CONSUMING lane. Deliberately NOT part of `affected` and NOT referenced by any target's dependsOn or by targetDefaults — it runs ONLY on an explicit `nx run apigen-cli:e2e` (and nx affected never selects it). It runs the sibling *.e2e.ts suites via vitest.e2e.config.ts. `dependsOn` is exactly the resource prerequisites those suites need: `build` (most spawn the built dist/index.js) and `apigen-java:package` (java-ts-decimal-parity drives the JVM).",
      "dependsOn": ["build", "apigen-java:package"],
      "outputs": ["{workspaceRoot}/coverage/entrypoint/apigen-cli-e2e"],
      "options": {
        "cwd": ".",
        "command": "vitest run --config {projectRoot}/vitest.e2e.config.ts"
      },
      "cache": false
    },
    "generate-api": {
      "executor": "@adhd/apigen-nx:generate",
      "cache": true,
      "outputs": ["{workspaceRoot}/tmp/apigen-generate-out"],
      "inputs": ["default", "^production"],
      "dependsOn": ["build"],
      "options": {
        "source": "src/test/fixtures/real-api.ts",
        "type": "mcp",
        "outDir": "tmp/apigen-generate-out"
      }
    },
    "nx-release-publish": {
      "dependsOn": [
        "build",
        "test",
        "verify-dist-load",
        "dist-manifest",
        "publish-hygiene"
      ],
      "options": {
        "packageRoot": "{projectRoot}/dist"
      }
    },
    "bench": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --expose-gc --import tsx packages/apigen/tools/bench-extract.ts",
        "cwd": "{workspaceRoot}",
        "env": {
          "TSX_TSCONFIG_PATH": "tsconfig.base.json"
        }
      },
      "cache": false
    }
  }
}
