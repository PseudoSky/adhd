# PRESERVATION SNAPSHOT — 2026-09-24

These are **verbatim, byte-for-byte snapshots** of work that existed only locally
(untracked on every ref, or committed only on an unpushed branch) as of
2026-09-24. They were captured onto this branch solely so that this at-risk work
exists on a remote and would survive a local tree cleanup.

**This branch is a preservation artifact, not a change proposal.** Do not merge
it as a feature. It is not a fork or a divergence — it is a backup.

## Snapshotted files

| Path | Origin of the authoritative copy |
| --- | --- |
| `docs/decisions/README.md` | Primary tree (untracked) |
| `docs/decisions/0001-adhd-stores-migrate-to-sox-store-adapter.md` | Primary tree (untracked) |
| `docs/decisions/0002-correct-the-source-never-work-around.md` | Primary tree (untracked) |
| `docs/decisions/TEMPLATE.md` | Primary tree (untracked) |
| `entrypoint/backlog/report/TRANSITION-STATUS.md` | Primary tree working file; also committed on `fix/g1-embedding-live-config` |
| `entrypoint/backlog/report/notes-addendum-scoping.md` | Primary tree (untracked) |

## Authoritative copies

The authoritative copies live in the **primary tree** at
`/Users/nix/dev/node/adhd`, and — for `TRANSITION-STATUS.md` — on the unpushed
branch `fix/g1-embedding-live-config`. Consult those, not this branch, for any
further edits. This snapshot is frozen and will not track later changes.
