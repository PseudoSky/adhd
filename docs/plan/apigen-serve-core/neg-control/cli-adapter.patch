diff --git a/packages/apigen/apigen-plugin-cli-output/src/lib/run.ts b/packages/apigen/apigen-plugin-cli-output/src/lib/run.ts
index dffe1869..071fa476 100644
--- a/packages/apigen/apigen-plugin-cli-output/src/lib/run.ts
+++ b/packages/apigen/apigen-plugin-cli-output/src/lib/run.ts
@@ -443,7 +443,7 @@ class CliTransportAdapter implements TransportAdapter<CliRawCall> {
     }
     // BUG-APIGEN-015 parity: `undefined` (a void op) becomes `null` — canonical
     // JSON, never the bare word `undefined` (not valid JSON output on a wire).
-    console.log(JSON.stringify(result === undefined ? null : result));
+    console.log(result === undefined ? null : result);
   }
 
   writeError(_raw: CliRawCall, err: unknown, _plan: OpPlan): void {
