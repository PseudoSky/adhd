# UNRESOLVED — dispatch-completion Demo

Interfaces this demo had to guess (not yet built), and scope gaps found while authoring. Every `⟦U#⟧` here maps to *remaining* dispatch-completion work — it is the acceptance target the implementer must satisfy. Resolve each before treating the corresponding DEMO.md step as authoritative.

## Unresolved interfaces

| ID | Guessed interface | Used in | Basis | What would confirm it |
|----|-------------------|---------|-------|-----------------------|
| U1 | `validate` rejects an unknown `dispatch_log[].provider` with a provider-path error | 1.2 | inferred from DEBT-DISPATCH-019 (enum not enforced today; `'teammate'` passes 25/25 spec tests) | extend `DispatchLogEntry.provider` union in dispatch-base-spec + enforce in `validate.ts`; a negative test |
| U2 | `snapshot.optimization.context_window_per_tier` stays finite across `JSON.parse(JSON.stringify(...))` | 2.2 | inferred from DEBT-DISPATCH-014 (`Infinity`→`null` latent) | reject/clamp absent tiers at `snapshot()` entry + a round-trip test |
| U3 | `DispatchUnit.execution_mode` field (`generative`/`tool-call`/`guard-only`) on `optimize` output | 3.1 | inferred from DEBT-DISPATCH-005 BL-102 / `ExecutionMode` (field absent in live spec — verified) | add `ExecutionMode` to dispatch-base-spec + derive in `optimize()`'s `assembleUnit()` |
| U4 | `dispatch optimize --enrich io,gitnexus` flag + the two plugins raising `pairwise_overlap`/`blast_radius` | 3.2 | inferred — `@adhd/dispatch-plugin-io` and `-plugin-gitnexus` unbuilt; `IOptimizerDeps` injection seam exists | build both plugins + wire the `--enrich` flag into the CLI/optimizer path |
| U5 | `snapshot.milestones[x].eligible` flips to `false` once `x` is complete | 4.2 | inferred from DEBT-DISPATCH-013 (D-07 defines `eligible` without own-completion; field never flips) | promote own-completion into the spec `eligible` definition; a completed-dag snapshot test |
| U6 | `dispatch run --runner-fault fire` seam + a recorded `status:"failed"` log entry on mid-cycle failure | 4.3 | inferred from DEBT-DISPATCH-015 (no error boundary in `orchestrateCycle`); `--runner-fault` is a demo affordance | add a per-unit try/catch that records a `failed` entry; a fault-injection test |
| U7 | `dispatch run --to-terminal` loop + causal-replan rewiring downstream `depends_on` onto an injected correction | §4 climax | inferred from DEBT-DISPATCH-020 (replan is generic, doesn't rewire; resume dead-ends at `no-eligible-work`) | causal-aware replan milestone + a resume-to-terminal test; confirm the `--to-terminal` flag name |
| U8 | `calibrate` builds no `AgentMcpRunner` on the bad-tier reject path (lazy factory) | 5.1 | inferred from DEBT-DISPATCH-024 (eager runner construction; inert today) | thread a lazy runner factory into `calibrateCore`; a factory-spy test |
| U9 | `snapshot`/`status`/`run` report a missing dag file with the offending path, like `validate` does | 5.2 | inferred from DEBT-DISPATCH-025 (only `validateCore` guards gracefully today) | extract `validateCore`'s guard into a shared helper the other `*Core` fns call |
| U10 | `@adhd/dispatch-tools` MCP surface (`dag.milestone_add` / `dispatch tools milestone-add`) rejecting cycle-forming edits | 5.3 | inferred — the dag-authoring MCP tools are unbuilt; DagClient is the CRUD authority they must wrap | build `@adhd/dispatch-tools`; confirm the exact tool/command names + arg shape |
| U11 | Op-level `type:"automated"`/`action:"guard"` executes through the guard seam | 5.4 | inferred from DEBT-DISPATCH-016 (orchestrator never reads `op.guard`/`op.type`/`op.action`) | route op-level guards through `GuardExecFn`; a non-duplicated op-guard test |
| U12 | `dispatch snapshot --serializer sqlite:<path>` + `@adhd/dispatch-serializer-sqlite` adapter parity with JSON | 6.1 | inferred — the SQLite serializer + `--serializer` flag are unbuilt; `IDagSerializer` contract exists | build the SQLite adapter to the existing `IDagSerializer`; a cross-serializer equality test; confirm the flag syntax |
| U13 | `npx @adhd/dispatch-cli status …` resolves a real `bin` | 6.2 | inferred from DEBT-DISPATCH-022 (no `bin` field; `bin/cli.ts` not compiled by the vite build) | add `bin` field + esbuild `build-bin` target; an npx spawn smoke test |
| U14 | Four new nx projects exist and are green: `dispatch-serializer-sqlite`, `dispatch-plugin-io`, `dispatch-plugin-gitnexus`, `dispatch-tools` | 6.3 | inferred — none exist today | scaffold the packages with test+build targets; `nx run-many` exits 0 |
| U15 | The optimizer's algorithm cascade (Bitmask DP / Tree DP / SA / HLFET) behind `dispatch-core-optimizer` | 6.3 | inferred — greedy packer only ships today; cascade is data-gated | build the cascade **only** once ≥3 real cycles show >15% greedy shortfall (P7); else record `HELD` |

## Scope gaps & open questions
- **None open.** All dispatcher decisions are resolved by the coordinator: single merged plan; slug `dispatch-completion`; **DEBT-DISPATCH-006 (per-turn `task_events`) handed off to the agent-* stream (no agent-mcp touch)**; **the two former headline gaps — tool-call execution (BUG-DISPATCH-EXEC-001) and package-name/publish conformance (BUG-DISPATCH-PUBLISH-001) — are being fixed DIRECTLY against `packages/dispatch/*` and are OUT of this plan's scope** (landed preconditions; Phase 0 triage confirms them); `optimizer-algorithms` ships HELD/non-terminal-blocking; terminal is release-ready (human-gated `nx release publish`); the apigen generated-CLI `$ref` crash stays deferred to `packages/apigen/BACKLOG.md`.
- **Note (not a gap):** beats 1.1, 2.1, 2.2 (eligibility), 2.3, 3.1 (packing), 4.1, 5.1 (message+exit) are grounded live against the shipped CLI on 2026-07-15 and pass today; they are included so the demo exercises the *whole* subsystem as one story, giving the completion work its acceptance frame.
