# UNRESOLVED — agent-final Demo

Interfaces this demo had to guess (not yet built), and scope gaps found while authoring.
Every `⟦U#⟧` maps to *remaining* agent-final work — resolve each before treating the
corresponding DEMO.md step as authoritative. The two nested demos keep their own ledgers
(dispatch-completion: 15 items; store-move: 5 items); nothing here duplicates them.

## Unresolved interfaces

| ID | Guessed interface | Used in | Basis | What would confirm it |
|----|-------------------|---------|-------|-----------------------|
| U1 | Authoring/discovery tool names + shapes on the host surface: `component_define {name,type,content,shared?}` → `{version,changed,summary}`, `component_read`, `component_delete`, `component_consumers`, `INVALID_TYPE` on unknown prompt type, bounded default output caps | 2.4, 1.1, 5.2(sweep), 5.3(sweep) | designed in superseded/agent-mcp-authoring (README.md:122-129, contexts/component-define.md:9-31) — **plan never executed**; tool names from its SPEC §5.2/§6 | the authoring lane lands on the built server; `tools/list` over real stdio shows the tools; the response shapes match or this demo's literals get corrected |
| U2 | `agent_define {name,model,components[{name,context_condition?}],tools[]}` → `{changed,composed_prompt_id,compiled_preview}`; typed `COMPONENT_NOT_FOUND` rollback; compat rule `systemPrompt` ⊕ `components` → `VALIDATION_ERROR` | 1.2, 1.3, 1.4 | superseded/agent-mcp-authoring decisions.md:167-186 + contexts/agent-define.md:9-24,55-58 (transactional upsert design, unbuilt) | implementation + a rollback test proving the registry is byte-identical after a failed define |
| U3 | `component_search` served by sox `SqliteSearchBackend` over `kind:'generic'` graph-store nodes (sub-kind in meta per BL-295 Option A), response `{results[{name,score,summary}],degraded}`; `@adhd/sox-graph-store`/`-hybrid-search` present as published deps of `agent-store-prompts` | 2.1, 2.2 | decisions.md §D6 ⟲FLIP (Option A, owner directive 2026-07-11); sox BL-295 Option-A contract + `degraded` field landed in sox 2026-07-16 (fixer commits `65dad22`, BL-295 close in flight); **publish is in flight, npm still 404 at authoring time** | `npm view @adhd/sox-graph-store version` exits 0; `agent-store-prompts/package.json` declares both; the rank-1 semantic-match assertion holds against the real fused backend |
| U4 | `agent_compile {name,platform,context}` tool + response fields `cache: HIT\|MISS` and `composed_prompt_id`; yaml-frontmatter vs json emit selected by platform column | 3.1, 3.2, §4 | the compile/cache machinery is verified real at the library layer (SYNTHESIS §1.3: `agent-engine-compiler`, SHA-256-keyed `registry_composed_prompts`); the MCP tool wrapper + exact response fields are from the unbuilt authoring plan (`_shared.md:134` lists `agent_compile` among the 11 discovery tools) | the tool lands; a HIT/MISS pair against the same inputs and a drift MISS after a component bump, asserted from the wire response |
| U5 | Budget plugin config keys `{"module":"@adhd/agent-plugin-budget","config":{"maxModelCalls":1}}` via `ADHD_AGENT_PLUGINS`, producing a structured budget-limit task failure | 4.2 | the by-name loading seam is verified (`loader.ts:260-281`, env var + config.json both real); `maxModelCalls`/`maxToolCalls` named in the plugin's package description and RatePolicyPlugin's pure-config shape (arch doc §2E) — exact env-var JSON schema unverified; **note: the plugin's own 35-test suite has never run (OBS-2)** | wire the plugin's `test` target and run its suite; a live task run with the cap set to 1 that fails on call 2 with the documented error shape |
| U6 | `dispatch-cli run --runner in-process` flag + an in-process client path that writes tasks into the same `DATABASE_PATH` store the MCP host uses; post-consolidation `agent-runner.ts` free of mirrored wire types (today at `:19-102`) and of the bare-`agent_create` path (today at `:353-373`) | 5.1, 5.2, §4 | arch doc §3b owner-stated target ("dispatch on the same client, in-process; wire runner survives for remote only") + owner-settled deletion list (SYNTHESIS §Settled); the flag name and the client factory (`createAgentEngineClient()` — also §3b, currently nonexistent) are inferred | the seam milestone lands; the flag (or its real name) exists in `dispatch-cli --help`; a dispatched unit's task row appears in the shared store; the greps in 5.2 return 0 against the real file |

## Scope gaps & open questions

- **Policy/enforcement has no beat — deliberately.** Owner ruling O-1 (2026-07-16): `agent-core-policy` was intended as agent restrictions (tools/files/access), "may be irrelevant at this point," on hold. This demo asserts nothing about policy gating; if a future ruling revives enforcement, this demo gains an act and this gap closes.
- **Act 2 has a cross-repo landed-precondition:** sox publish of `graph-store` + `hybrid-search` with the BL-295 Option-A contract closed (in flight via sox-fixer at authoring time). Same treatment as the nested dispatch demo's §2.3 preconditions — confirmed at Phase 0, not re-planned here.
- **The demo harness (`call-tool.mjs`, `seed-registry.mjs`) is demo infrastructure the plan builds** — a real stdio MCP client per AGENTS.md §"drive the real tools, never a bypass" (JSON-RPC to the unmodified built server; never imports of server internals).
- **The live-model halves of 4.1 gate on `AGENT_MCP_LIVE=1`** — the one legitimate env-gate (paid third-party model), per AGENTS.md §"Live testing is mandatory"; the structural halves run unflagged.
- Nested ledgers stay authoritative for their milestones: `../store-move/UNRESOLVED.md` (5 items, incl. the drizzle-config path) and `../superseded/dispatch-completion/demo/UNRESOLVED.md` (15 items). No item is duplicated into this file.
