# UNRESOLVED — agent-final Demo

Interfaces this demo had to guess (not yet built), and scope gaps found while authoring.
Every `⟦U#⟧` maps to *remaining* agent-final work — resolve each before treating the
corresponding DEMO.md step as authoritative. The two nested demos keep their own ledgers
(dispatch-completion: 15 items; store-move: 5 items); nothing here duplicates them.

## Unresolved interfaces

| ID | Guessed interface | Used in | Basis | What would confirm it |
|----|-------------------|---------|-------|-----------------------|
| U1 | Authoring/discovery tool names + shapes on the host surface: `component_define {name,type,content,shared?}` → `{version,changed,summary}`, `component_read`, `component_delete`, `component_consumers`, `INVALID_TYPE` on unknown prompt type, bounded default output caps | 2.4, 1.1, 5.2(sweep), 5.3(sweep) | designed in superseded/agent-mcp-authoring (README.md:122-129, contexts/component-define.md:9-31) — **plan never executed**; tool names from its SPEC §5.2/§6 | the authoring lane lands on the built server; `tools/list` over real stdio shows the tools; the response shapes match or this demo's literals get corrected |
| U2 | `agent_define {name,model,components[{name,context_condition?}],tools[]}` → `{changed,composed_prompt_id,compiled_preview}`; typed `COMPONENT_NOT_FOUND` rollback; compat rule `systemPrompt` ⊕ `components` → `VALIDATION_ERROR` | 1.2, 1.3, 1.4 | superseded/agent-mcp-authoring decisions.md:167-186 + contexts/agent-define.md:9-24,55-58 (transactional upsert design, unbuilt) | implementation + a rollback test proving the registry is byte-identical after a failed define |
| U3 | `component_search` served by sox `SqliteSearchBackend` over `kind:'generic'` graph-store nodes (sub-kind in meta per BL-295 Option A), response `{results[{name,score,summary}],degraded}`; `@adhd/sox-graph-store`/`-hybrid-search` present as published deps of `agent-store-prompts` | 2.1, 2.2 | decisions.md §D6 ⟲FLIP (Option A, owner directive 2026-07-11); sox BL-295 Option-A contract + `degraded` field landed in sox 2026-07-16 (fixer commits `65dad22`, BL-295 close in flight); **publish LANDED 2026-07-16** (`sox-graph-store@0.3.0`, `sox-hybrid-search@0.2.0`, `npm view` verified — OBS-22) | `npm view @adhd/sox-graph-store version` exits 0; `agent-store-prompts/package.json` declares both; the rank-1 semantic-match assertion holds against the real fused backend |
| U4 | `agent_compile {name,platform,context}` tool + response fields `cache: HIT\|MISS` and `composed_prompt_id`; yaml-frontmatter vs json emit selected by platform column | 3.1, 3.2, §4 | the compile/cache machinery is verified real at the library layer (SYNTHESIS §1.3: `agent-engine-compiler`, SHA-256-keyed `registry_composed_prompts`); the MCP tool wrapper + exact response fields are from the unbuilt authoring plan (`_shared.md:134` lists `agent_compile` among the 11 discovery tools) | the tool lands; a HIT/MISS pair against the same inputs and a drift MISS after a component bump, asserted from the wire response |
| U5 | Budget plugin config keys `{"module":"@adhd/agent-plugin-budget","config":{"maxModelCalls":1}}` via `ADHD_AGENT_PLUGINS`, producing a structured budget-limit task failure | 4.2 | the by-name loading seam is verified (`loader.ts:260-281`, env var + config.json both real); `maxModelCalls`/`maxToolCalls` named in the plugin's package description and RatePolicyPlugin's pure-config shape (arch doc §2E) — exact env-var JSON schema unverified; **note: the plugin's own 35-test suite has never run (OBS-2)** | wire the plugin's `test` target and run its suite; a live task run with the cap set to 1 that fails on call 2 with the documented error shape |
| U6 | `dispatch-cli run --runner in-process` flag + an in-process client path that writes tasks into the same `DATABASE_PATH` store the MCP host uses; post-consolidation `agent-runner.ts` free of mirrored wire types (today at `:19-102`) and of the bare-`agent_create` path (today at `:353-373`) | 5.1, 5.2, §4 | arch doc §3b owner-stated target ("dispatch on the same client, in-process; wire runner survives for remote only") + owner-settled deletion list (SYNTHESIS §Settled); the flag name and the client factory (`createAgentEngineClient()` — also §3b, currently nonexistent) are inferred | the seam milestone lands; the flag (or its real name) exists in `dispatch-cli --help`; a dispatched unit's task row appears in the shared store; the greps in 5.2 return 0 against the real file |
| U7 | The MERGED provider registry: one canonical schema absorbing dispatch's `ProviderConfig` (`dispatch-base-spec/src/lib/types.ts:388-395`), agent-mcp's `providerConfigSchema` (`agent-engine-orchestrator/src/validation/agent.ts:87-91`, currently a *required* agent field at `:106-120`), and `agent-core-provider`'s tables; `vendor/model` slug wire format derived from `provider_model_platform_bindings`; `agentDefinitionSchema.provider` becomes an optional `model_hint`; new `sessions.provider`/`sessions.model` columns; resolution chain `task ?? session ?? agent ?? global` | 6.1, 6.2, 6.3 | owner ruling D-G(1)(2) 2026-07-17 ("we can merge all of the registries" + inheritance-chain selection); registry shape verified closest-to-target in OBS-24 (schema already separates provider × model × binding × tool-format; gap is wiring) — exact merged schema, slug derivation, and column names are design work | the provider milestone's schema lands; `agent_create` accepts an agent with no provider; a session row shows inherited provider/model; the 3-literal `switch` in `factory.ts:8-26` is deleted in favor of registry lookup |
| U8 | `session_update {session_id, model?\|models?}` as the swap/routing surface, and soft-swap mechanics: on provider change, prior history + tool schemas re-render through the registry's tool-format layer (`emitTool()`, `provider_tool_formats` — built+tested, zero seed rows, unwired per `emit-tools.ts:11-14`) | 6.2, 6.3 | owner ruling D-G(3); opencode's `/models` mid-session reselection is the only prior art found and its carryover semantics are undocumented (opencode.ai/docs/models) — the tool name, arg shape, and re-render trigger point are inferred | the swap tool lands on the host surface; the BLUEHERON beat passes against a real cross-provider swap; the negative control (normalization stubbed → hard fail) proves the re-render path executes |
| U9 | Session-level ordered fallback `models: string[]` tried in order on provider error, with the failed attempt visible in an attempt trace / usage ledger rather than silently absorbed | 6.3 | owner ruling D-G(4), modeled on OpenRouter's `models[]` semantics (ordered, last-error-wins when exhausted — openrouter.ai/docs/api-reference/overview); adhd has zero fallback machinery today (`factory.ts:21-24` throws) — field name, attempt-trace shape, and error classification (auth vs transient) are design work | the routing milestone lands; revoked-key beat completes via provider 2 with the anthropic attempt recorded; removing the list makes the same beat fail (negative control) |

## Scope gaps & open questions

- **Policy/enforcement has no beat — deliberately.** Owner ruling O-1 (2026-07-16): `agent-core-policy` was intended as agent restrictions (tools/files/access), "may be irrelevant at this point," on hold. This demo asserts nothing about policy gating; if a future ruling revives enforcement, this demo gains an act and this gap closes.
- **Act 2 has a cross-repo landed-precondition:** sox publish of `graph-store@0.3.0` + `hybrid-search@0.2.0` with the BL-295 Option-A contract closed — **landed 2026-07-16, registry-verified** (OBS-22). Same treatment as the nested dispatch demo's §2.3 preconditions — confirmed at Phase 0, not re-planned here.
- **No driver harness exists — the runner is a real MCP host session** (owner directive 2026-07-17: "I dont like how your demo doesnt use the actual mcp/clis"). Every `mcp__agent-mcp__*` line is a literal loaded-tool invocation via `.mcp.json` → the built dist; server-scoped env (plugins, provider secrets, `AGENT_MCP_LIVE`) is set in `.mcp.json`'s env block + `/mcp` reload — which the shim had been hiding as fake per-call flags.
- **The live-model halves of 4.1 gate on `AGENT_MCP_LIVE=1`** — the one legitimate env-gate (paid third-party model), per AGENTS.md §"Live testing is mandatory"; the structural halves run unflagged.
- **Dispatcher items are covered first-class** (owner directive 2026-07-17): the nested demo's full REQ/CAP set is imported into DEMO.md §7.2b as `DSP-*` rows, and its 15 unresolved-work items are indexed here for scope visibility — detail stays in `../superseded/dispatch-completion/demo/UNRESOLVED.md` (authoritative): DSP-U1 provider-enum validation · DSP-U2 Infinity-safe snapshot round-trip · DSP-U3 `execution_mode` on DispatchUnit · DSP-U4 `--enrich` io/gitnexus plugins · DSP-U5 snapshot `eligible` flips on completion · DSP-U6 `--runner-fault` recorded-not-thrown seam · DSP-U7 `--to-terminal` causal replan · DSP-U8 lazy runner factory in calibrate · DSP-U9 consistent missing-dag-file errors · DSP-U10 `@adhd/dispatch-tools` MCP surface · DSP-U11 op-level guard routing · DSP-U12 SQLite serializer + `--serializer` flag · DSP-U13 real npx `bin` · DSP-U14 four new nx projects green · DSP-U15 data-gated optimizer cascade. (Written without the ⟦ ⟧ glyph deliberately — their stub ids live in the nested ledger, not this one.)
- Nested ledger for the runtime milestone stays authoritative likewise: `../store-move/UNRESOLVED.md` (5 items, incl. the drizzle-config path).
