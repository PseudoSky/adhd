# UNRESOLVED — store-move demo

| ID | Guessed interface | Used in | Basis | What would confirm it |
|----|-------------------|---------|-------|-----------------------|
| U1 | `drizzle-kit generate --config entrypoint/agent-mcp/drizzle.config.ts` — a drizzle config file exists and covers both packages' schemas | beats 1, 3 | `ls entrypoint/agent-mcp/drizzle.config.*` returned nothing in-session; migrations exist (0000–0009) so *something* generates them, but the config path/name is unverified | locate the real config (check `package.json` scripts, `drizzle.config.js` at repo root or in the package); run `generate` on the untouched baseline and confirm beat 1's FK-drop output before relying on beat 3 |
| U2 | `@adhd/agent-store-runtime` will export `AgentStore` from its index after the fold | beat 5 | the package exports its other stores from `src/index.ts` (SessionStore/TaskStore pattern); the moved class following suit is convention, not fact | the fold implementation adds the export + a consumer smoke test importing it from the built dist |
| U3 | `chat-gateway.ts` (`entrypoint/agent-mcp/src/streaming/chat-gateway.ts`) uses `AgentStore` in a way that survives the import repoint unchanged | beat 2 | grep showed it references the store; the usage was not read | read the file during implementation; if it reaches into store internals rather than the public class surface, that is a finding to surface, not silently adapt |
| U4 | A 0009-upgraded DB and a fresh post-fold DB have bit-identical `sessions`/`agents` DDL (constraint of GOAL.md) | beat 3 | 0009's rebuilt DDL was read and matches the planned `.references()` emission *by inspection*; drizzle's exact emitted DDL text (quoting, clause order) was not diffed | `sqlite3 <db> ".schema sessions"` diff between the two DBs as an implementation gate |
| U5 | The two `AgentStore` classes (runtime one moving into `agent-store-runtime`; registry one in `agent-store-prompts/src/store/agent-store.ts` keyed by slug) can coexist in one dependency graph without import ambiguity | beats 2, 5 | different packages, different tables (`agents` vs `registry_agents`) — but identical class name; SYNTHESIS §1.2 documents the duplication | implementation decides: rename on move (e.g. `RuntimeAgentStore`) or keep and rely on package-scoped imports; either way the demo's beat-5 import must be unambiguous. Renaming is a (wire-invisible) API choice — flag to owner if renamed |

No open scope questions — decomposition beyond plan 1 deliberately deferred by the owner
("Lets make 1 right now then come back", 2026-07-16).
