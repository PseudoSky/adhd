{
  "target-artifact-parity": {
    "anchor": "packages/data/data-query-engine/project.json",
    "rule": "a target converted to inference must still emit the same artifact at the same path as the explicit target it replaced",
    "discovered_via": "manual",
    "shared_ref": "[ref:target-artifact-parity]",
    "audit_check": "audit-final.ref-target-artifact-parity"
  },
  "fail-safe-fast-path": {
    "anchor": "docs/plan/nx-23-upgrade/scripts/check-zero-selection.mjs",
    "rule": "the narrow fast path exits non-zero when it selects zero tests; the default path is never narrowed",
    "discovered_via": "manual",
    "shared_ref": "[ref:fail-safe-fast-path]",
    "audit_check": "audit-final.ref-fail-safe-fast-path"
  }
}
