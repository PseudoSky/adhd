{
  "$schema": "http://json-schema.org/schema",
  "$id": "scan",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["staged", "range", "all"],
      "description": "staged = git index (pre-commit); range = base..head diff (CI); all = every tracked file (audit).",
      "default": "staged"
    },
    "base": {
      "type": "string",
      "description": "Range mode only: the base revision."
    },
    "head": {
      "type": "string",
      "description": "Range mode only: the head revision."
    }
  },
  "required": [],
  "additionalProperties": false
}
