# @adhd/nx-build

Build-lifecycle plugin: `link`, `verify-dist-load`, `publish-hygiene`, `publish` executors + `detect-target` util. Targets are inferred onto every buildable project; wiring lives in plugin.js/targetDefaults, never in project.json.
